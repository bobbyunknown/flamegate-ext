{
  "slug": "cline",
  "alias": "cl",
  "aliases": ["cl", "cline"],
  "name": "Cline",
  "version": "0.1.0",
  "description": "Cline extension via WASM. Uses Cline's OpenAI-compatible chat completions API with WorkOS OAuth.",
  "entrypoints": {
    "chat": "invoke",
    "models": "list_models"
  },
  "auth_modes": ["oauth"],
  "timeout": 120,
  "default_account_key": "default"
}